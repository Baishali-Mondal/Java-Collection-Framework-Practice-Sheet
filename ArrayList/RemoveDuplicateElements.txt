import java.util.ArrayList;
import java.util.HashSet;
import java.util.LinkedHashSet;
import java.util.Set;

public class RemoveDuplicateElements {
    static void main(String[] args) {

        ArrayList<Integer> list = new ArrayList<>();
        list.add(10);
        list.add(35);
        list.add(20);
        list.add(5);
        list.add(5);
        list.add(10);
        list.add(10);
        list.add(35);
        list.add(5);
        list.add(35);
        list.add(35);

        Set<Integer> uniqueList = new HashSet<>(list);// HashSet doesn't preserve the order of the list

        Set<Integer> uniqueorderList = new LinkedHashSet<>(list); //LinkedHashSet preserve the order of the list

        list.clear();   // [] it clears the list
        list.addAll(uniqueList);// it modifies the same list with the new data


        System.out.println(list);
//        System.out.println(uniqueList);
//        System.out.println(uniqueorderList);
    }
}